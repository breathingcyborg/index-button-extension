/**
 * ServiceAccountCreds
 * 
 * Only contains the properties we use.
 * Could have more properties.
 * 
 */
export type ServiceAccountCreds = {
    project_id: string,
    private_key_id: string,
    private_key: string,
    client_email: string,
}
